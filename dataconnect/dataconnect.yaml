specVersion: "v1beta"
serviceId: "tondefi"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tondefi-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
