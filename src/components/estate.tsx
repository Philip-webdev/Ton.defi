import React from 'react';

export interface EstateProps {
    // define your props here
}

const Estate: React.FC<EstateProps> = (props) => {
    return (
        <div>
            {/* Estate component content */}
        </div>
    );
};

export default Estate;